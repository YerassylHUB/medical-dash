{
  "name": "dash-medical-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix front\"",
    "dev:front": "npm run dev --prefix front",
    "dev:front:fresh": "npm run dev --prefix front",
    "dev:api": "npm run dev --prefix backend",
    "build": "npm run build --prefix front",
    "build:api": "npm run build --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
